# 🛡️ Матрица доступа и RLS для схем Supabase

Матрица показывает, какие роли PostgreSQL имеют доступ к каким схемам и таблицам в вашем self-hosted Supabase, а также как они взаимодействуют с Row Level Security (RLS).

| Роль | Схема `public` (Пользовательские данные) | Схема `auth` (Аутентификация) | Схема `storage` (Файлы) | Схема `realtime` (WebSocket) | Схема `vault` (Секреты) | Схема `drizzle` (Миграции) | **Пояснение / Механизм работы** |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **`anon`** | ✅ **Чтение/Запись (по RLS)** | ❌ Нет прямого доступа | ✅ **Чтение/Запись (по RLS)** | ❌ Нет прямого доступа | ❌ Нет доступа | ❌ Нет доступа | **Публичный доступ через API.** Полностью контролируется политиками RLS в `public` и `storage`. Не имеет прямого доступа к служебным схемам. |
| **`authenticated`** | ✅ **Чтение/Запись (по RLS)** | ❌ Нет прямого доступа | ✅ **Чтение/Запись (по RLS)** | ❌ Нет прямого доступа | ❌ Нет доступа | ❌ Нет доступа | **Зарегистрированный пользователь.** Отличается от `anon` только JWT-токеном (`role=authenticated`). Доступ также определяется RLS. |
| **`service_role`** | ✅ **Полный доступ (bypass RLS)** | ✅ **Полный доступ (bypass RLS)** | ✅ **Полный доступ (bypass RLS)** | ✅ **Полный доступ (bypass RLS)** | ✅ **Полный доступ (bypass RLS)** | ✅ **Полный доступ (bypass RLS)** | **Привилегированный сервисный ключ.** Обходит все RLS-политики (`rolbypassrls=true`). Используется для серверных скриптов, админских функций и внутренней работы Supabase. **НИКОГДА не раскрывайте на клиенте.** |
| **`supabase_admin`** | ✅ **Полный доступ (SUPERUSER)** | ✅ **Полный доступ (SUPERUSER)** | ✅ **Полный доступ (SUPERUSER)** | ✅ **Полный доступ (SUPERUSER)** | ✅ **Полный доступ (SUPERUSER)** | ✅ **Полный доступ (SUPERUSER)** | **Главная админская роль (`rolsuper=true`).** Имеет неограниченные права на всё в БД (как классический `postgres`). Используется для первоначальной настройки и обслуживания. |
| **`authenticator`** | ➡️ **Посредник (Switcher)** | ➡️ **Посредник (Switcher)** | ➡️ **Посредник (Switcher)** | ❌ Нет прямого доступа | ❌ Нет доступа | ❌ Нет доступа | **Роль-переключатель.** Не имеет собственных прав. Получает JWT от клиента, проверяет его и **МЕНЯЕТ** свою текущую роль на `anon` или `authenticated` в рамках сессии, предоставляя им доступ согласно RLS. |
| **`supabase_auth_admin`** | ❌ Нет доступа | ✅ **Полный доступ** | ❌ Нет доступа | ❌ Нет доступа | ❌ Нет доступа | ❌ Нет доступа | **Администратор аутентификации.** Настроена на работу только в схеме `auth` (`search_path=auth`). Управляет таблицами пользователей, сессий, провайдеров. |
| **`supabase_storage_admin`** | ❌ Нет доступа | ❌ Нет доступа | ✅ **Полный доступ** | ❌ Нет доступа | ❌ Нет доступа | ❌ Нет доступа | **Администратор хранилища.** Настроена на работу только в схеме `storage` (`search_path=storage`). Управляет бакетами и объектами. |
| **`supabase_realtime_admin`** | ❌ Нет доступа | ❌ Нет доступа | ❌ Нет доступа | ✅ **Полный доступ** | ❌ Нет доступа | ❌ Нет доступа | **Администратор Realtime.** Управляет внутренними таблицами (`subscription`, `messages`) для работы WebSocket-сервиса. |
| **`postgres`** | ✅ **Полный доступ (bypass RLS)** | ✅ **Полный доступ (bypass RLS)** | ✅ **Полный доступ (bypass RLS)** | ✅ **Полный доступ (bypass RLS)** | ✅ **Полный доступ (bypass RLS)** | ✅ **Полный доступ (bypass RLS)** | **Переименованная стандартная роль.** Имеет `rolbypassrls=true`, но не является суперпользователем (`rolsuper=false`). Используется для внутренних операций, схожих с `service_role`. |
| **`supabase_read_only_user`** | ✅ **Только чтение (глобально)** | ✅ **Только чтение (глобально)** | ✅ **Только чтение (глобально)** | ✅ **Только чтение (глобально)** | ❓ Зависит от политик | ✅ **Только чтение (глобально)** | **Роль для аналитики/мониторинга.** Все её транзакции по умолчанию только для чтения (`default_transaction_read_only=on`). Полезна для подключения BI-инструментов без риска изменений. |
| **`pgbouncer`** | ➡️ Для пула соединений | ➡️ Для пула соединений | ➡️ Для пула соединений | ➡️ Для пула соединений | ➡️ Для пула соединений | ➡️ Для пула соединений | **Роль для пулера соединений PgBouncer.** Используется для входа в БД с целью пулинга. Сама по себе не имеет прав, конечные права определяются ролью подключившегося клиента. |
| **`pg_*` (системные)** | ✅/❌ Зависит от роли | ✅/❌ Зависит от роли | ✅/❌ Зависит от роли | ✅/❌ Зависит от роли | ✅/❌ Зависит от роли | ✅/❌ Зависит от роли | **Системные роли PostgreSQL.** Например, `pg_read_all_data` имеет право читать все во всех схемах, но не может менять данные. Используются для предоставления специфичных привилегий без создания суперпользователя. |

---

## 🔐 Ключевые принципы модели безопасности Supabase

1.  **Разделение обязанностей (SoD):** Административные роли (`*_admin`) ограничены своими схемами. `auth_admin` не может трогать `storage`, и наоборот.
2.  **Посредник (Authenticator Pattern):** Клиенты никогда не подключаются напрямую как `anon`/`authenticated`. Все запросы идут через роль `authenticator`, которая, проверив JWT, переключает контекст безопасности. Это основа RLS.
3.  **RLS vs Bypass RLS:**
    *   **RLS (`anon`, `authenticated`):** Каждая строка в таблице проверяется против SQL-политик (`CREATE POLICY ...`). Клиентские ключи используют этот путь.
    *   **Bypass RLS (`service_role`, `postgres`):** Политики игнорируются. Сервисные ключи используют этот путь для действий, которые должны быть вне политик (например, создание пользователя в `auth.users`).
4.  **Суперпользователь (`supabase_admin`):** Используется только для DDL-операций (создание таблиц, расширений), глобальной настройки и аварийного восстановления. Не используется в работе приложения.
5.  **Схема `vault`:** Доступ к `vault.secrets` обычно ограничен ролями, которые управляют ключами (например, `supabase_admin`), или осуществляется через специальные функции `pgsodium`, которые проверяют права внутри себя. Прямой SELECT из неё для обычных ролей должен быть запрещён.

---

## 🗺️ Типичный путь запроса через API Supabase
Клиент (JWT) -> API (Kong) -> PostgREST -> База данных (PostgreSQL)
| | | |
| | | [1. Подключение как authenticator]
| | | [2. JWT верифицируется]
| | | [3. Роль меняется на anon/authenticated]
| | | [4. Выполняется запрос к public.table]
| | | [5. RLS-политики фильтруют строки]
| | | [6. Результат возвращается клиенту]


**Обозначения в матрице:**
*   ✅ **Полный доступ (bypass RLS):** Чтение, запись, удаление; политики RLS игнорируются.
*   ✅ **Полный доступ (SUPERUSER):** Как выше, плюс DDL-права (CREATE, DROP), управление ролями и т.д.
*   ✅ **Чтение/Запись (по RLS):** Операции разрешены, но каждая строка проверяется активными политиками RLS.
*   ✅ **Только чтение (глобально):** Любой оператор изменения данных (`INSERT`, `UPDATE`, `DELETE`) будет отвергнут СУБД.
*   ➡️ **Посредник (Switcher):** Роль не имеет самостоятельных прав, выступает переключателем на другие роли.
*   ❌ **Нет прямого доступа:** У роли нет привилегий (`GRANT`) на объекты в данной схеме. Запросы будут завершаться ошибкой "permission denied".
*   ❓ **Зависит от политик:** Доступ может быть предоставлен через дополнительные настройки или функции.