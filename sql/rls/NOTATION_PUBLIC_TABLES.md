# 📊 Таблицы схемы Public в Supabase

Ниже представлена таблица с описанием назначения таблиц в схеме `public` вашего self-hosted Supabase.

| Таблица | Назначение |
| :--- | :--- |
| **project_create_requests** | Запросы на создание новых проектов. |
| **branches** | Ветки проектов (например, для разработки, staging, production). |
| **project_invitations** | Приглашения пользователей в проекты. |
| **projects** | Основная информация о проектах. |
| **project_settings** | Настройки конкретных проектов. |
| **legacy_subscriptions** | Устаревшие записи о подписках (исторические данные). |
| **products** | Товары или услуги, доступные для подписки. |
| **rate_limits** | Настройки и квоты лимитов использования (Rate Limiting). |
| **subscriptions** | Активные подписки пользователей на продукты/услуги. |
| **user_projects** | Связь многие-ко-многим между пользователями и проектами (членство). |
| **usage_records** | Записи об использовании услуг (для биллинга и аналитики). |
| **user_settings** | Персональные настройки пользователей. |
| **users** | Основная информация о пользователях. |
| **user_canvases** | Связь пользователей с рабочими областями ("холстами"). |
| **project_custom_domains** | Пользовательские домены, привязанные к проектам. |
| **prices** | Цены на продукты/услуги (тарифы). |
| **canvas** | Рабочие области или "холсты" (например, для визуального конструктора). |
| **frames** | Кадры или элементы внутри холста (canvas). |
| **deployments** | Записи о развертываниях (деплоях) проектов. |
| **conversations** | Диалоги или треды сообщений. |
| **messages** | Отдельные сообщения внутри диалогов. |
| **custom_domains** | Общий реестр пользовательских доменов. |
| **custom_domain_verification** | Данные для верификации владения пользовательскими доменами. |
| **preview_domains** | Домены для предпросмотра проектов. |
| **feedbacks** | Обратная связь от пользователей. |

---


### **Основные сущности: Проекты и пользователи**
*   **`projects`, `users`**: Центральные таблицы. Скорее всего содержат базовые поля: `id` (первичный ключ), `name`, `created_at`, `updated_at`, `status`. В `users` также могут быть `email`, `encrypted_password`.
*   **`user_projects`**: Связующая таблица. Поля: `user_id` (внешний ключ на `users.id`), `project_id` (внешний ключ на `projects.id`), `role` (роль пользователя в проекте, например, 'owner', 'member').

### **Управление доступом и настройками**
*   **`project_invitations`**: Поля: `email`, `project_id`, `token` (для ссылки-приглашения), `invited_by_user_id`, `status` ('pending', 'accepted'), `expires_at`.
*   **`project_settings`, `user_settings`**: Поля вида `entity_id` (ссылка на проект/пользователя) и различные колонки с настройками (`key`/`value` или отдельными полями).

### **Биллинг и подписки (Stripe-like модель)**
*   **`products`**: `name`, `description`, `active` (boolean).
*   **`prices`**: `product_id` (ссылка на товар), `unit_amount` (цена), `currency`, `interval` ('month', 'year' для подписки), `active`.
*   **`subscriptions`**: `user_id` или `project_id`, `price_id`, `status` ('active', 'canceled', 'past_due'), `current_period_start`, `current_period_end`, `cancel_at_period_end`.
*   **`usage_records`**: `subscription_id`, `quantity` (потребленные единицы), `timestamp`.

### **Домены и развертывание**
*   **`custom_domains`, `project_custom_domains`**, **`preview_domains`**: Поля: `domain` (имя домена), `project_id`, `status` ('pending', 'active', 'error'), `verified_at`.
*   **`custom_domain_verification`**: Может содержать `domain_id` и `verification_token` или `dns_record`.
*   **`deployments`**: `project_id`, `branch` (имя ветки), `status` ('building', 'live', 'failed'), `log_url`, `created_at`.

### **Ветки и контент (Canvas)**
*   **`branches`**: `project_id`, `name` (имя ветки), `is_default` (boolean).
*   **`canvas`, `user_canvases`, `frames`**: Поля для управления визуальным контентом. В `canvas` может быть `project_id`, `name`; в `frames` - `canvas_id`, `type`, `properties` (JSON).

### **Коммуникации и обратная связь**
*   **`conversations`**: `title`, `created_by`, `status`.
*   **`messages`**: `conversation_id`, `author_id`, `content`, `created_at`.
*   **`feedbacks`**: `user_id`, `subject`, `message`, `category`.

### **Вспомогательные таблицы**
*   **`project_create_requests`**: Может содержать `user_id`, `project_name`, `specifications`, `status`.
*   **`rate_limits`**: `project_id`, `feature` (например, 'api_calls'), `limit`, `period`.
*   **`legacy_subscriptions`**: Поля, аналогичные `subscriptions`, для данных из старой системы.