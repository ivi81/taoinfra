# 🔁 Таблицы схемы Realtime в Supabase

Ниже представлена таблица с описанием таблиц в схеме `realtime`, которая является частью **Supabase Realtime** — сервиса для подписки на изменения в базе данных через WebSocket.

| Таблица | Назначение |
| :--- | :--- |
| **`subscription`** | Основная таблица для управления активными WebSocket-подписками клиентов на изменения в базе данных. |
| **`messages`** | Таблица для хранения исходящих сообщений (изменений), которые нужно отправить подписанным клиентам. |
| **`schema_migrations`** | Служебная таблица для контроля версий схемы базы данных самого сервиса Realtime. |

---

# 📝 Описание назначения полей таблиц Realtime

> **Примечание:** Эти таблицы являются внутренними для работы сервиса Realtime. Их данные автоматически управляются сервисом, и вручную их изменять обычно не требуется.

### **`realtime.subscription` (Управление подписками)**
Эта таблица отслеживает, какие клиенты (через WebSocket соединения) на что подписаны.

| Поле (Предполагаемое) | Тип (Предполагаемый) | Описание |
| :--- | :--- | :--- |
| **`id`** | `BIGINT` или `UUID` | Уникальный идентификатор записи о подписке. |
| **`subscription_id`** | `TEXT` | Уникальный идентификатор подписки, сгенерированный для клиента (используется для фильтрации). |
| **`entity`** | `TEXT` | Сущность, на которую подписан клиент. Часто это комбинация схемы и таблицы (например, `"public.posts"`). |
| **`filters`** | `JSONB` | Условия фильтрации подписки. Определяет, на какие конкретно изменения должен реагировать клиент (например, `{"column": "id", "value": "123"}`). |
| **`claims`** | `JSONB` | JWT-claims (утверждения) пользователя, установившего подписку. Используется для авторизации и RLS. |
| **`created_at`** | `TIMESTAMPTZ` | Время создания подписки. |

### **`realtime.messages` (Очередь исходящих сообщений)**
Эта таблица действует как очередь для сообщений об изменениях, которые нужно передать подписчикам.

| Поле (Предполагаемое) | Тип (Предполагаемый) | Описание |
| :--- | :--- | :--- |
| **`id`** | `BIGINT` | Уникальный идентификатор сообщения. |
| **`topic`** | `TEXT` | Тема (канал) сообщения. Обычно соответствует полю `entity` из таблицы `subscription`. |
| **`event`** | `TEXT` | Тип события, которое произошло в БД: **`INSERT`**, **`UPDATE`** или **`DELETE`**. |
| **`payload`** | `JSONB` | **Самое важное поле.** Содержит полные данные об изменении, включая новые (`record`) и старые (`old_record`) значения строк. |
| **`metadata`** | `JSONB` | Дополнительные метаданные о событии (например, время возникновения, идентификатор транзакции). |
| **`created_at`** | `TIMESTAMPTZ` | Время поступления сообщения в очередь. |

### **`realtime.schema_migrations` (Управление миграциями)**
Стандартная для многих приложений служебная таблица.

| Поле (Предполагаемое) | Тип (Предполагаемый) | Описание |
| :--- | :--- | :--- |
| **`version`** | `VARCHAR` или `BIGINT` | Номер или идентификатор версии примененной миграции схемы. |
| **`inserted_at`** | `TIMESTAMPTZ` | Время применения миграции. |

---

# ⚙️ Как работает поток данных в Realtime

1.  **Триггер в вашей таблице:** Когда в любой таблице вашей БД (например, `public.posts`) происходит изменение (INSERT/UPDATE/DELETE), специальный триггер **`realtime`** фиксирует это.
2.  **Запись в `messages`:** Этот триггер вставляет новую строку в `realtime.messages` с темой (`topic="public.posts"`), типом события и данными (`payload`).
3.  **Рассылка по подпискам:** Сервис Realtime считывает новые сообщения из `realtime.messages`, находит в `realtime.subscription` всех клиентов, подписанных на тему `"public.posts"`, и чьи `filters` совпадают с данными в `payload`.
4.  **Отправка через WebSocket:** Отфильтрованное сообщение отправляется по соответствующему WebSocket-соединению каждому подходящему клиенту.
5.  **Получение на клиенте:** Ваше фронтенд-приложение (использующее Supabase Realtime client) получает это сообщение и может обновить интерфейс.

### 💡 Практический вывод для разработчика
*   **Не изменяйте данные в этих таблицах вручную**, если вы не занимаетесь глубокой отладкой или разработкой самого сервиса Realtime.
*   Если Realtime перестал отправлять события, проверьте логи контейнера `supabase-realtime` и убедитесь, что триггеры `realtime` существуют на нужных таблицах.
*   Таблицы в этой схеме — это **"движок"** Realtime. Ваши данные лежат в своих схемах (`public`, `auth` и др.), а Realtime лишь отслеживает и транслирует их изменения.