# 🗂️ Таблицы схемы Storage в Supabase

Ниже представлена таблица с описанием таблиц в схеме `storage`, которая является частью **Supabase Storage** — сервиса для управления файлами (объектное хранилище).

| Таблица | Назначение |
| :--- | :--- |
| **`buckets`** | Основная таблица для управления "ведрами" (контейнерами/папками верхнего уровня) для хранения файлов. |
| **`objects`** | Основная таблица для хранения метаданных всех загруженных файлов. |
| **`prefixes`** | Таблица для управления "виртуальными папками" внутри `buckets`, организующими объекты в иерархию. |
| **`s3_multipart_uploads`** | Управление сессиями многокомпонентной (multipart) загрузки больших файлов, совместимой с S3 API. |
| **`s3_multipart_uploads_parts`** | Хранение информации об отдельных загруженных частях (chunks) в рамках сессии `multipart_upload`. |
| **`migrations`** | Служебная таблица для контроля версий схемы базы данных сервиса Storage. |
| **`buckets_vectors`** | Таблица, связанная с векторными (эмбеддинговыми) расширениями для семантического поиска по содержимому бакетов (если включено). |
| **`vector_indexes`** | Индексы для ускорения векторного поиска по данным в `buckets_vectors`. |
| **`buckets_analytics`** | Таблица для сбора аналитики использования бакетов (например, количество операций, объем данных). |

---

# 📝 Описание назначения полей ключевых таблиц Storage

> **Примечание:** Эти таблицы являются внутренними для работы сервиса Storage. Взаимодействие с ними происходит через Storage API, а не напрямую через SQL.

### **`storage.buckets` (Управление контейнерами)**
"Ведро" (bucket) — это корневой контейнер для файлов, аналогичный папке или S3 Bucket.

| Поле (Предполагаемое) | Тип | Описание |
| :--- | :--- | :--- |
| **`id`** | `TEXT` | **Уникальный идентификатор (название) ведра** (например, `"avatars"`, `"project-documents"`). Используется в путях к файлам. |
| **`name`** | `TEXT` | Человекочитаемое отображаемое имя. |
| **`owner`** | `UUID` | Идентификатор владельца (`auth.users.id`). Может быть `NULL` для публичных бакетов. |
| **`created_at`** | `TIMESTAMPTZ` | Время создания. |
| **`updated_at`** | `TIMESTAMPTZ` | Время последнего обновления. |
| **`public`** | `BOOLEAN` | **Ключевая настройка доступа.** Если `true`, файлы в этом бакете доступны публично по URL без токена. |
| **`avif_autodetection`** | `BOOLEAN` | Настройка для автоматической конвертации изображений в формат AVIF. |
| **`file_size_limit`** | `BIGINT` | Максимально допустимый размер одного файла в байтах. |
| **`allowed_mime_types`** | `TEXT[]` | Массив разрешенных MIME-типов (например, `{'image/jpeg', 'image/png'}`). Если `NULL` — все типы разрешены. |

### **`storage.objects` (Метаданные файлов)**
Эта таблица хранит информацию о каждом загруженном файле, **но не сами файлы**. Файлы физически хранятся в S3-совместимом бэкенде или на диске сервера.

| Поле (Предполагаемое) | Тип | Описание |
| :--- | :--- | :--- |
| **`id`** | `UUID` | Уникальный идентификатор объекта (файла) в системе. |
| **`bucket_id`** | `TEXT` | **Ссылка на ведро.** Внешний ключ в `storage.buckets(id)`. |
| **`name`** | `TEXT` | **Полный путь к файлу внутри ведра**, включая префиксы (например, `"users/avatar_123.png"`). |
| **`owner`** | `UUID` | Идентификатор пользователя-владельца файла (`auth.users.id`). |
| **`created_at`** | `TIMESTAMPTZ` | Время загрузки файла. |
| **`updated_at`** | `TIMESTAMPTZ` | Время последнего обновления метаданных. |
| **`last_accessed_at`** | `TIMESTAMPTZ` | Время последнего чтения файла (может обновляться не всегда). |
| **`metadata`** | `JSONB` | **Гибкое поле для пользовательских метаданных.** Сюда можно сохранять авторство, описание, теги и т.д. |
| **`path_tokens`** | `TEXT[]` | **Техническое поле.** Массив, полученный разбиением `name` по `/` (например, `{"users", "avatar_123.png"}`). Используется для эффективного поиска по путям. |
| **`version`** | `TEXT` | Версия объекта (для контроля версий файлов, если функция включена). |
| **`mime_type`** | `TEXT` | MIME-тип файла (например, `"image/png"`), определенный при загрузке. |
| **`size`** | `BIGINT` | Размер файла в байтах. |

### **`storage.prefixes` (Организация папок)**
Виртуальные папки, выведенные в отдельную таблицу для эффективной организации и контроля доступа.

| Поле (Предполагаемое) | Тип | Описание |
| :--- | :--- | :--- |
| **`id`** | `UUID` | Уникальный идентификатор префикса (папки). |
| **`bucket_id`** | `TEXT` | Ведро, к которому относится папка. |
| **`name`** | `TEXT` | Имя папки (например, `"documents"`). Полный путь строится рекурсивно. |
| **`owner`** | `UUID` | Владелец папки. |
| **`created_at`** | `TIMESTAMPTZ` | Время создания. |
| **`parent_id`** | `UUID` | **Ссылка на родительскую папку** (в этой же таблице). Для корневых папок в бакете — `NULL`. |

### **`storage.s3_multipart_uploads` & `_parts` (Загрузка больших файлов)**
Таблицы для поддержки потоковой загрузки файлов по частям (как в Amazon S3).

| Таблица / Поле | Описание |
| :--- | :--- |
| **`s3_multipart_uploads`** | Хранит информацию о **сессии** загрузки. Поля: `id` (ключ сессии), `bucket_id`, `object_path`, `upload_id`, `created_at`, `in_progress`. |
| **`s3_multipart_uploads_parts`** | Хранит информацию о **загруженных частях**. Поля: `upload_id` (ссылка на сессию), `part_number`, `etag` (хеш части), `size`. После загрузки всех частей система собирает из них итоговый файл. |

---

# ⚙️ Как это работает вместе: пример загрузки файла

1.  **Создание бакета:** Через API или Studio создается бакет `storage.buckets` (например, `id: "projects"`).
2.  **Запрос на загрузку:** Клиент отправляет файл в бакет `"projects"` по пути `"blueprints/final-v1.pdf"`.
3.  **Проверка прав:** Storage API проверяет RLS-политики для `buckets` и `objects`.
4.  **Создание записи:** Если доступ есть, в `storage.objects` вставляется новая строка:
    *   `bucket_id: "projects"`
    *   `name: "blueprints/final-v1.pdf"`
    *   `owner: [user_uuid]`
    *   `metadata: {"author": "Alice", "version": "1.0"}`
5.  **Сохранение файла:** Бинарные данные файла сохраняются в выбранный бэкенд (диск, S3).
6.  **Доступ по URL:** Файл становится доступен по URL вида: `https://[project-ref].supabase.co/storage/v1/object/public/projects/blueprints/final-v1.pdf` (если бакет публичный).

### 💡 Практический вывод для разработчика
*   **RLS (Row Level Security) действует и здесь.** Все операции через API проверяются через политики, определенные для таблиц `storage.buckets` и `storage.objects`.
*   **`storage.objects.name` — это ключ.** Именно это поле используется для построения URL и поиска файлов.
*   **Пользовательские метаданные (`metadata`)** — мощный инструмент для связывания файлов с сущностями вашего приложения и организации сложного поиска.
*   Для прямой работы с файлами **всегда используйте Storage API или клиентские библиотеки Supabase**, а не прямые SQL-запросы к этим таблицам.